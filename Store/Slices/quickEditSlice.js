import { createSlice } from "../Y-state";

const quickEditSlice = createSlice({
    name : "quickEdit",
    initialState : {
        
    },
    reducers : {

    }
})



export const {  } = quickEditSlice.actions;
export default quickEditSlice.reducer;